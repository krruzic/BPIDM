<UserControl x:Class="BPIDM.Controls.BPMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BPIDM.Controls"
             mc:Ignorable="d" x:Name="BPMenuItemControl">
    <Grid>
        <Border Name="mask" Background="White" CornerRadius="20"/>
            <Grid  Background='{StaticResource "BPRed"}'>
                <!-- Use a VisualBrush of 'mask' as the opacity mask -->
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=mask}"/>
            </Grid.OpacityMask>
            <Grid Margin="10,10,10,10">
                <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="5*"/>
                    </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="{Binding DisplayedName, ElementName=BPMenuItemControl}" HorizontalAlignment="Center" Foreground='{StaticResource "BPWhite"}' FontSize="24" VerticalAlignment="Top"/>
                <Image Grid.Row="1" Source="{Binding DisplayedImage, ElementName=BPMenuItemControl}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <Label Grid.Row="1" Content="{Binding DisplayedPrice, ElementName=BPMenuItemControl}" HorizontalAlignment="Right" Foreground='{StaticResource "BPWhite"}' FontSize="18" VerticalAlignment="Bottom" Opacity="0.7"/>
                </Grid>
            </Grid>
        </Grid>
</UserControl>
